{
  "name": "budget-tracker-front",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend",
    "start": "npm run preview --workspace frontend -- --host 0.0.0.0 --port ${PORT:-4173}"
  }
}
